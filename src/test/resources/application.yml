kestra:
  repository:
    type: memory
  queue:
    type: memory
  storage:
    type: local
    local:
      base-path: /tmp/unittest

micronaut:
  server:
    # -1 lets Micronaut pick a free port; MatrixExecutionTest injects the real URL into the test flows
    port: -1
